<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.guuda.sheep">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:name="com.guuda.sheep.application.SheepApplication"
        android:theme="@style/Theme.MySunSheep"
        tools:targetApi="31">

        <activity
            android:exported="true"
            android:name="com.guuda.sheep.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.guuda.sheep.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.guuda.sheep.GameActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.guuda.sheep.SettingActivity"
            android:screenOrientation="portrait"/>

    </application>

</manifest>